{% extends 'base.html.twig' %}

{% block title %}Maraude{% endblock %}

{% block body %}
    <h1>Maraude</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ maraude.id }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ maraude.adresse }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ maraude.type }}</td>
            </tr>
            <tr>
                <th>Organisme</th>
                <td>{{ maraude.organisme }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ maraude.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ maraude.description }}</td>
            </tr>
            <tr>
                <th>ImageFilename</th>
                <td>{{ maraude.imageFilename }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('maraude_index') }}">back to list</a>

    <a href="{{ path('maraude_edit', {'id': maraude.id}) }}">edit</a>

    {{ include('maraude/_delete_form.html.twig') }}
{% endblock %}
